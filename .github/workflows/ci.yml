name: CI

on:
  push:
    branches: [main, master, "claude/**", "codex/**"]
  pull_request:
    branches: [main, master]

jobs:
  backend-tests:
    name: Backend — pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r backend/requirements.txt pytest pytest-cov httpx pip-audit

      - name: Verify Python dependency consistency
        run: pip check

      - name: Audit Python dependencies
        run: pip-audit -r backend/requirements.txt --strict

      - name: Run tests
        run: pytest tests/ -v --cov=core --cov-report=term-missing

  frontend-lint-build:
    name: Frontend — lint + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: cd frontend && npm ci

      - name: Lint
        run: cd frontend && npm run lint

      - name: Test frontend request helpers
        run: cd frontend && npm run test

      - name: Build
        run: cd frontend && npm run build

      - name: Audit production dependencies
        run: cd frontend && npm audit --omit=dev --audit-level=high
        env:
          VITE_API_URL: http://localhost:8000
