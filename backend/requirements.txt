# Requires Python 3.9+
# Core API
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.23.0

# Validation
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0

# Physics / Thermochemistry
# Cantera requires system-level build dependencies (gcc, BLAS/LAPACK).
# Install guide: https://cantera.org/install
cantera>=3.0.0

# Numerics & Data
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Visualisation (optional, used by export endpoints)
plotly>=5.15.0

# HTTP server support
python-multipart>=0.0.6
